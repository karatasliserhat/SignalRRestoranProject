@model UpdateMenuTableDto
@{
    ViewData["Title"] = "Update";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}
<div class="content">
    <div class="container-fluid">
        <h4 class="page-title">Kategori İşlemleri</h4>
        <div class="row">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">Kategori Güncelleme Formu</div>
                    </div>
                    <form asp-action="Update" asp-controller="MenuTable" method="post">
                        <input type="hidden" asp-for="@Model.MenuTableId" />
                        <div class="card-body">
                            <div class="form-group">
                                <label asp-for="@Model.Name"></label>
                                <input type="text" class="form-control" asp-for="@Model.Name" placeholder="Masa Adı">
                            </div>
                            <div class="form-group">
                                <label asp-for="@Model.Status"></label>
                                <select class="form-control" asp-for="@Model.Status" asp-items="@ViewBag.TFList">
                                </select>
                            </div>
                            <div class="card-action">
                                <button class="btn btn-success">Güncelle</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

